fuel-mark-engine-plugin_controller-role:
  # Role name
  name: "MarK Controller"
  # Role description
  description: "OpenStack controller node deployed with help of MK24"
  # If primary then during orchestration this role will be
  # separated into primary-role and role
  has_primary: false
  # Assign public IP to node if true
  public_ip_required: false
  # Weight that will be used to sort out the
  # roles on the Fuel web UI
  weight: 10
fuel-mark-engine-plugin_compute-role:
  # Role name
  name: "MarK Compute"
  # Role description
  description: "OpenStack compute node deployed with help of MK24"
  # If primary then during orchestration this role will be
  # separated into primary-role and role
  has_primary: false
  # Assign public IP to node if true
  public_ip_required: false
  # Weight that will be used to sort out the
  # roles on the Fuel web UI
  weight: 10
fuel-mark-engine-plugin_config-role:
  # Role name
  name: "MarK Config"
  # Role description
  description: "SaltStack master node"
  # If primary then during orchestration this role will be
  # separated into primary-role and role
  has_primary: false
  # Assign public IP to node if true
  public_ip_required: false
  # Weight that will be used to sort out the
  # roles on the Fuel web UI
  weight: 10
